# cropsown-registry — build & publish on GitLab.
#
# Generated by openg2p/packaging ci/migrate/github-to-gitlab.sh, translated
# from this repo's GitHub caller. All versioning/build/publish logic lives
# centrally in openg2p/packaging@v1; this file only declares what THIS repo has.
#
#   images -> registry.gitlab.com/openg2p/registry/cropsown-registry/<name>   (this project)
#   chart  -> openg2p/charts Helm registry                (shared catalogue)

include:
  - project: 'openg2p/packaging'
    ref: v1
    file: '/ci/gitlab/build-publish.yml'

variables:
  PACKAGING_REF: v1

  IMAGES: |
    [
      {
        "name": "staff-api",
        "dockerfile": "docker/staff-api/Dockerfile"
      },
      {
        "name": "partner-api",
        "dockerfile": "docker/partner-api/Dockerfile"
      },
      {
        "name": "celery",
        "dockerfile": "docker/celery/Dockerfile"
      },
      {
        "name": "db-seed",
        "dockerfile": "docker/db-seed/Dockerfile"
      },
      {
        "name": "sanity-tests",
        "dockerfile": "docker/sanity-tests/Dockerfile"
      }
    ]

  CHART_PATH: helm/openg2p-cropsown-registry
  CHART_IMAGE_PATHS: '[".registry.staffApi.image.tag",".registry.partnerApi.image.tag",".registry.celeryWorker.image.tag",".registry.celeryBeat.image.tag",".registry.dbSeed.image.tag",".registry.sanity.image.tag"]'
  CHART_GITLAB_PROJECT: openg2p/charts
